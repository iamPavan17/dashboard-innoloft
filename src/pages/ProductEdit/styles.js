import styled from "styled-components";

const FormWrapper = styled.div`
  margin: 30px;
  display: flex;
  flex-direction: column;
  gap: 14px;

  button {
    align-self: flex-end;
  }
`;

export { FormWrapper };
